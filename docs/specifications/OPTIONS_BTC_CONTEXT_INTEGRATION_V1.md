# Daily BTC context and readiness integration

Task OPT-BTC-2, design reviewed September 7, 2026 UTC before implementation.
Use current agent settings; bounded integration under the Owner's existing public
factor collection, development, saving and Git authorization. No new source call
is required to integrate the already verified Coinbase observation.

## Daily context

Create an immutable v3 daily-restoration snapshot linked to the exact v2 bytes.
Preserve both v1 and v2 snapshots; retain all non-prompt automation fields.
Preserve the original news prompt and Treasury workflow under clearly scoped
subsections, then independently attempt one BTC `--report` and `--refresh` at
the existing daily 09:00 New York wake. Each flow failure is independent. BTC
remains a dated single-venue snapshot; routine price changes and expected later
display staleness do not by themselves require a notification.

The current shared heartbeat's armed/collection wrapper references v3. Keep the
same id, target, name, cadence, September 8 09:30-09:50 New York window and frozen
contract request scope. Public context refreshes run only in the daily branch,
never every minute during option collection. Restore v3 before touching study
artifacts at cutoff. No new automation, background daemon or standalone task.
Local Codex turns require available host/app and account allowance; an active
timer alone does not guarantee execution. Do not consume reset credits.

## Readiness v2 composition

Keep the v1 six-component engine and its exported recovery function unchanged.
Add a typed pure v2 wrapper that revalidates the exact recomputed v1 report and
appends one independently recovered BTC context component. A new script function
calls the unchanged v1 recovery, checks the fixed BTC store path and recovers the
existing journal without appending or networking. The CLI uses this v2 report;
v1 remains callable for reproducibility of older checkpoints and integrations.

BTC summaries retain source/product/context scope, retrieval counts, source and
receipt clocks, at-receipt issues, current display staleness and comparison
availability. Do not expose raw provider text or invent quantities, probability,
continuous coverage or an IBIT price conversion. Validate summary counts/status/
clocks against the recovered source report; missing/busy/corrupt BTC storage
cannot hide paper reviews or other sources. Keep each check clock and the v1
assessment clock; v2 is also non-atomic. New report version and fingerprint must
cover BTC evidence. All existing review calculations and mandatory dependencies
remain exactly those of v1; optional BTC context cannot qualify an option replay.

## Acceptance

Test v3 exact prior-snapshot linkage and unchanged non-prompt fields, original
news/Treasury preservation, one BTC daily refresh, identical wrapper phases and
unchanged collection JavaScript. Test v1 reproducibility, BTC isolation, source
and receipt clocks, data-only summary, failure transitions, no network/writes and
all prior journals unchanged. Run focused and full validation, independently
read back actual host fields after the authorized update, and save a dated
integration checkpoint. Never overwrite earlier delivery checkpoints or journals.

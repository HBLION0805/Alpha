# Daily decision milestone V1

Owner direction: September 16, 2026. Scope is one usable GLD/IBIT decision page
and one prospective actual-quote paper round trip, with recoverable review.
The Owner reports that Codex was not running on Monday and was only restarted
today. Weekend option-market closure is expected; public news still requires a
running collector. This explanation is attributed to the Owner, not inferred
from missing receipts. At restart the prior gld-ibit automation was independently
reported absent by the app; its deletion cause is unknown.

## Acceptance

1. Existing fixed public readers recover at actual clocks, followed by a natural
   hourly refresh. Show stale/missing sources and regular-session closure clearly.
   Restore the authorized ongoing market heartbeat, without the expired close
   study or cancelled routine opening/midday samples. Configuration is not proof
   of future execution.
2. The existing Daily guidance page opens with one compact decision card per ETF:
   current action, attributed directional view, primary supporting/conflicting
   evidence, quote and analysis times, event posture, at most two budget-compatible
   reference contracts, and explicit entry/exit/invalidation conditions. Derive
   these from existing verified readers and calculators. Unknown global fees stay
   unknown; a separately labeled dated-fee cost example cannot promote WATCH or
   qualify execution. English UI. Existing issued reports remain reproducible.
3. Register before the window, observe later actual source quotes, model entry
   and independent exit with declared costs, save the result and candidate review,
   and independently recover it. NO_ENTRY, a synthetic result, a schedule, or an
   unresolved position cannot satisfy this acceptance. Snapshot-modeled fills
   are not broker fills or proof of profitability. Strict execution qualification
   and statistical validation remain separate, open requirements.

## Bounded rehearsal

Use September 17 existing hourly wakes at 10:20 and 11:20 New York for a one-day
engineering paper collection exception, whether or not a major event is listed.
No new recurrence times. Keep the original 24-call / 36-quote / six-tracked-ID
bounds, actual-time slot claims and immutable capture-to-paper processing.
Register one IBIT October 16 $43 put from the September 16 saved source, one
contract, $2.10 maximum entry/share, $0.10 maximum spread/share, $0.01 exit
allowance/share, existing dated fee assumption, 20% stop / net 2R. Entry window
10:15–10:40; time exit 11:10; existing market-session monitoring cutoff. Selection
is for pipeline testing, not a bearish recommendation. Require all unchanged
snapshot checks; never manufacture fills or loosen a condition to get closure.
An ineligible entry requires a new prospective plan, not editing the old one.

## Scope control and validation

Preserve $100–$500 per-trade allocation / $1,000 declared equity, all old captures,
settings, paper plans, journals, and unapproved lessons. No orders, account tools,
paid source, quota reset, or continuous quote polling. Do not expand the factor
catalog or add a new dashboard. The backend projection and existing UI change
together. Test stale/partial sources, attribution sequencing, unknown costs,
budget filtering, escaping, and paper recovery; inspect desktop/mobile layout.
Record actual acceptance separately from planned work. Completion requires all
three outcomes; the active goal stays open while market evidence is pending.

Review: this composes existing engines and a bounded future paper observation;
no new trading signal or price model is introduced. The Owner approved this
milestone and continued implementation in the current task.

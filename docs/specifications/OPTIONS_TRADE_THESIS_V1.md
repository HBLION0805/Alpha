# Trade thesis and independent exits V1

Owner-authorized local extension, September 19, 2026. Reuse the manual ledger's
hash-linked, exclusive append and protected preview/save API, Position watch's
existing stop/target/time/expiry checks, saved ETF evidence and macro comparisons.
No collector, schedule, allocation, frozen experiment or order changes.

New registrations may carry an optional versioned thesis: selected timing template,
trade date, realization window, next check, event exposure declaration, manual
fallback and only the enabled event/price/manual conditions. Original time exit,
stop and target remain the sole exit parameters. Drafts are append-only ledger
records; confirmation is an immutable registration, never a replacement. Existing
registrations and report fingerprints without new commands retain V1 behavior.

Conditions compare declared numeric event releases, exact ETF/option boundaries,
or explicitly sourced Owner confirmations. Numeric arithmetic is exact. Closed-bar
conditions use the existing five-minute validator and source qualification; imported
or conflicted historical bars cannot become qualified by this feature. Source and
receipt clocks must precede evaluation; a later revision cannot replace an earlier
release identity. Each condition has its own evidence gate. Pending verification
is separate from missing evidence after the check time. No new default threshold,
holding duration or automatic opening check is inferred from the timing template.

Thesis SUPPORTED/WARNING/INVALIDATED/UNKNOWN is separate from exit action. Any
independent stop, target, time, expiry or reliable invalidation takes precedence
over unrelated unknown evidence. Hold requires all relevant checks evaluable and
no trigger. Quotes never establish fills. Explicitly saved assessments copy the
plan fingerprint, evidence and clocks; saved triggers remain visible thereafter.
Corrections append explanations and never erase original judgments. Page refresh
does not append. Owner-reported action is separate from ledger fill reports.

Verify the seven Owner acceptance cases with isolated fixtures, protected API,
restart recovery, unchanged old reports and actual English browser interaction.
Run strict TypeScript, the existing full validator and diff checks. Production
opening coverage and the separate first natural 15:50 receipt remain unverified.

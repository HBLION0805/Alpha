# Paper windows matched to the existing collection plan

The paper preview and frozen-plan frontend now compare entry and time-exit
windows with the dated routine schedule and saved major-event dates. The form no
longer advertises the cancelled 09:50/12:50 reads. A single routine 15:50 wake
cannot supply two separate observations for a same-day lifecycle. Conditional
event reads remain conditional; stale calendars cannot establish an event pair.

Use `options:snapshot-paper -- --preview-collection <request-file>` for the same
read-only comparison. Original `--preview`, registrations, fills and saved report
fingerprints remain unchanged. The protected frontend preview awaits its saved
calendar read. Current plan displays are later planning comparisons, not a new
interpretation of historical fill evidence. The 16:20 full-chain study is excluded
because it is not a source consumed by this paper adapter.

The September 12 Owner continuation registered `pipeline-check-ibit-20260915` as
an engineering rehearsal, not a directional recommendation. It selects the
existing IBIT October 9 $43 call identity. The source selection quote is dated
September 11 and cannot serve as its future entry. Frozen New York times:
September 15 entry 14:10–14:40, modeled time exit 15:40, final cutoff 16:20.
Nominal coverage is the conditional 14:20 event wake and routine 15:50 wake.
Actual source and receipt clocks remain authoritative; no delivery is promised.

One contract, $2.50 per-share entry limit, $0.10 maximum entry spread, $0.01
per-share exit allowance, the existing dated estimated fee profile, 20% premium
stop and net 2R assumptions are frozen. The $100–$500 allocation policy remains.
The initial report has zero fills; the enrolled identity fits the existing six-ID
tracking limit. No source call, timer or automation field was added or changed.
No-entry, open exposure, modeled wins/losses and candidate reviews use the existing
observation/finalization workflow. No approved lesson or real-price gate advanced.

See [design](specifications/OPTIONS_PAPER_COLLECTION_PLAN_V1.md) and
[actual activation and validation](status/paper-collection-plan.json).

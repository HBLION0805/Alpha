# First opening collection: verified handoff

Read-only operational audit: **2026-09-07T08:10:57.002Z**.
Code baseline: `23fe13f`, pushed to `codex/gld-ibit-options-foundation`.
The [checkpoint](status/pre-opening-handoff.json) binds a new local artifact;
accepted earlier reports retain their original clocks and bytes.

## What is ready

The actual collector preflight returns **WAIT**, no requested tools, zero attempts,
zero frames and zero usable observations. Its next request is
**2026-09-08T13:30:00.000Z**, or **September 8, 09:30 New York**. The frozen
twenty-minute window ends at 09:50 New York. The plan fingerprint still matches
`8e60f2a53ea47be83c30024e20d7a8ae1cc63fe1dca69ba8bc0e8540420f5c8d`.
No out-of-window quote was requested to manufacture progress.

All eight stored fields of the existing `gld-ibit` heartbeat matched the armed
phase: identity, kind, name, prompt, schedule, active state, target task and
notification policy. The active host SHA-256 remains
`66c1e3d5640ee8cc9e746aa90800beb613010829d9b3f0800d224e3576b72f6f`.
Twenty-seven protected source/study/report/snapshot files also matched.
This checks stored configuration; it does not prove a future scheduler wake.

Recovered local evidence:

| Component | Actual audit result | Meaning |
| --- | --- | --- |
| Frozen option collection | Available; window pending | No real opening path yet |
| Local paper reviews | Five closed, five reviewed, zero missing | Simulated trades only |
| Paper candidate notebook | Four candidates | No automatic strategy change |
| Independent historical research | Available; fifteen candidate lessons | Accounts remain separate; candidates are not established causes |
| Imported external history | Missing | No purchased/owner-provided file is implied |
| Headlines, Treasury, BTC | Stores available | Availability is not freshness or a calibrated signal |
| BLS and FOMC calendars | Available; thirteen selected entries | Twelve timed releases and one date-only interval |

No store was blocked. The recovered deterministic next step is
`WAIT_FOR_FROZEN_OPENING_WINDOW`. The new handoff wrapper saved
`data/runtime/options-readiness/pre-opening-handoff-20260907.json` without
overwriting existing evidence or changing any report version.

## What the scheduled run must do

Use the existing [runbook](OPTIONS_ROBINHOOD_AUTOCOLLECTION_RUNBOOK.md),
[phase fields](OPTIONS_ROBINHOOD_HEARTBEAT_PHASES.json) and immutable
[v5 restoration snapshot](OPTIONS_MARKET_CONTEXT_HEARTBEAT_RESTORE_V5.json).
These remain the execution instructions; this handoff does not replace them.

1. Daily 09:00 context checks the six public headline feeds, Treasury real yields,
   Coinbase BTC context, BLS release calendar and FOMC date calendar independently.
2. During the frozen September 8 window, only the existing heartbeat temporarily
   changes to a one-minute target cadence. It requests four frozen option quotes
   and GLD/IBIT equity quotes. Actual source, request, receipt and recording times
   remain separate. Tool errors retain fixed codes, without credential/error bodies.
3. At or after the end, restore v5 daily scheduling before reading collection
   logs, then save the closeout. If the entire window was missed, record missing
   coverage; do not backfill times, swap contracts or freeze a new plan automatically.

The computer, app, workspace, network and Codex execution capacity must be
available. Keeping the computer awake alone is insufficient. The local direct
tool route was [unsuccessful](OPTIONS_HOST_CONTROL_ASSESSMENT.md); it does not
provide an independent collector. Existing twenty-second MCP tool timeout
configuration is [verified](OPTIONS_HOST_TIMEOUT_ASSESSMENT.md), but effective
runtime enforcement and scheduling latency remain unmeasured. No reset credit,
paid capacity, new automation or background service is authorized by this audit.

## What remains before a real-price paper test

| Dependency | Evidence still required |
| --- | --- |
| Prospective observations | Actual opening source responses and receipt quality, including gaps/failures |
| Option quote semantics | Independent evidence about what `updated_at` covers, side/size timing, delay and retention; a recent timestamp alone cannot establish these |
| Executable cost model | Frozen fee, slippage, price-grid, whole-contract and adverse-fill assumptions under the existing risk limits |
| Robinhood-specific paper adapter | A reviewed source-specific adapter with explicit unknown/missing handling; no relabeling as Cboe |
| Outcome analysis | Every modeled outcome and unresolved position retained, with candidate reviews; no inferred 80% probability or size escalation |

The first opening collection evaluates data and scheduling. It is not a complete
trade, a win-rate test or proof that displayed size could be filled. The earlier
historical sample was entirely interpolated and cannot supply a missing bid/ask
path. Current source semantics, costs and account eligibility remain unverified;
account/order calls remain outside the authorized scope.

After collection, inspect the saved closeout and its candidate operational
lessons before selecting the next adapter task. Preserve the existing NO_REPLAY
gate until that separate qualification and implementation are complete.

## Validation and Git

This audit changed only this document, the new checkpoint and focused handoff/
README links. It ran actual local collector preparation, readiness recovery,
calendar recovery, host field comparison and protected-byte checks. No network
call, source append, host mutation, account/order access or trade occurred.
No implementation changed, so the already passed **3,107/3,107 tests** across
120 components were not repeated. Local links, JSON parsing and Git whitespace
were checked before the authorized documentation commit and push.

Unresolved risk is future runtime availability and unqualified market evidence,
not a hidden failed test. The current stores are assumed to remain in the same
local workspace; this artifact is not an off-device backup. The next step remains
the existing scheduled collection, followed by source-quality review.

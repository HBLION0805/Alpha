# Synthetic Treasury XML

`real-yields.synthetic.xml` models the reviewed public schema. Every yield, ID,
source date and source clock is invented for software tests. It is not a Treasury
response, market evidence, historical vintage or trading input. Runtime source
responses stay in the separate ignored Treasury journal.

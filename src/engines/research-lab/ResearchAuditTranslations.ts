import { AIAuditRecordType, AIAuditSourceSubsystem, type AIAuditRecordInput, type ResearchAuditRecord, type ResearchAuditTranslationContext } from "../../contracts";
function safeCode(value: string, index: number): string { const normalized = value.trim().replace(/[^A-Za-z0-9._:-]+/g, "_").slice(0, 180); return normalized.length === 0 ? `RESEARCH_REASON_${String(index + 1)}` : normalized; }
export function auditRecordFromResearch(source: Readonly<ResearchAuditRecord>, context: Readonly<ResearchAuditTranslationContext>): AIAuditRecordInput {
  return { schemaVersion: "1.0", recordId: source.auditId, idempotencyKey: context.idempotencyKey, recordType: AIAuditRecordType.ResearchRecord, sourceSubsystem: AIAuditSourceSubsystem.Research, sourceRecordId: source.sourceRecordId, ...(source.researchVersion === undefined ? {} : { sourceRecordVersion: source.researchVersion }), timestamp: source.timestamp, correlationId: source.correlationId, traceId: source.traceId, parentAuditRecordIds: structuredClone(context.parentAuditRecordIds), relatedAuditRecordIds: structuredClone(context.relatedAuditRecordIds), ledgerEntryIds: [], policyVersions: structuredClone(source.policyVersions), status: source.status, reasonCodes: source.reasonCodes.map(safeCode), actor: structuredClone(context.actor), privacyLevel: source.privacyLevel, retention: source.retention, metadata: { ...structuredClone(source.metadata), research_operation: source.operationType, research_id: source.researchId ?? null, evidence_reference_count: source.evidenceReferences.length }, payloadIntegrityReference: `research:${source.sourceRecordId}`, sourceAuditReferences: [source.auditId], finalOutcome: source.status };
}

import { AIAuditRecordType, AIAuditSourceSubsystem, type AIAuditRecordInput, type StrategyAuditRecord, type StrategyAuditTranslationContext } from "../../contracts";
function safeCode(value: string, index: number): string { const normalized = value.trim().replace(/[^A-Za-z0-9._:-]+/g, "_").slice(0, 180); return normalized.length === 0 ? `STRATEGY_REASON_${String(index + 1)}` : normalized; }
export function auditRecordFromStrategy(source: Readonly<StrategyAuditRecord>, context: Readonly<StrategyAuditTranslationContext>): AIAuditRecordInput { return { schemaVersion: "1.0", recordId: source.auditId, idempotencyKey: context.idempotencyKey, recordType: AIAuditRecordType.StrategyVersion, sourceSubsystem: AIAuditSourceSubsystem.StrategyVersioning, sourceRecordId: source.sourceRecordId, ...(source.semanticVersion === undefined ? {} : { sourceRecordVersion: source.semanticVersion }), timestamp: source.timestamp, correlationId: source.correlationId, traceId: source.traceId, parentAuditRecordIds: structuredClone(context.parentAuditRecordIds), relatedAuditRecordIds: structuredClone(context.relatedAuditRecordIds), ledgerEntryIds: [], policyVersions: structuredClone(source.policyVersions), status: source.status, reasonCodes: source.reasonCodes.map(safeCode), actor: structuredClone(context.actor), privacyLevel: source.privacyLevel, retention: source.retention, metadata: { ...structuredClone(source.metadata), strategy_operation: source.operationType, strategy_definition_id: source.definitionId ?? null, strategy_version_id: source.versionId ?? null, owner_approval_id: source.ownerApprovalId ?? null, evidence_reference_count: source.evidenceReferences.length }, payloadIntegrityReference: `strategy:${source.sourceRecordId}`, sourceAuditReferences: [source.auditId], finalOutcome: source.status }; }

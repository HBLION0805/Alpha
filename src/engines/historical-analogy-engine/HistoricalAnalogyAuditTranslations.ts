import { AIAuditRecordType, AIAuditSourceSubsystem, type AIAuditRecordInput, type HistoricalAnalogyAuditRecord, type HistoricalAnalogyAuditTranslationContext } from "../../contracts";

function safeCode(value: string, index: number): string { const normalized = value.trim().replace(/[^A-Za-z0-9._:-]+/g, "_").slice(0, 180); return normalized.length === 0 ? `ANALOGY_REASON_${String(index + 1)}` : normalized; }
export function auditRecordFromHistoricalAnalogy(source: Readonly<HistoricalAnalogyAuditRecord>, context: Readonly<HistoricalAnalogyAuditTranslationContext>): AIAuditRecordInput { return { schemaVersion: "1.0", recordId: source.auditId, idempotencyKey: context.idempotencyKey, recordType: AIAuditRecordType.HistoricalAnalogy, sourceSubsystem: AIAuditSourceSubsystem.HistoricalAnalogyEngine, sourceRecordId: source.sourceRecordId, ...(source.recordVersion === undefined ? {} : { sourceRecordVersion: source.recordVersion }), timestamp: source.timestamp, correlationId: source.correlationId, traceId: source.traceId, parentAuditRecordIds: structuredClone(context.parentAuditRecordIds), relatedAuditRecordIds: structuredClone(context.relatedAuditRecordIds), ledgerEntryIds: [], policyVersions: structuredClone(source.policyVersions), status: source.status, reasonCodes: source.reasonCodes.map(safeCode), actor: structuredClone(context.actor), privacyLevel: source.privacyLevel, retention: source.retention, metadata: { ...structuredClone(source.metadata), analogy_operation: source.operationType, analogy_request_id: source.requestId ?? null, current_snapshot_id: source.snapshotId ?? null, candidate_id: source.candidateId ?? null, weight_profile_id: source.profileId ?? null, weight_profile_version: source.profileVersion ?? null, scoring_method_version: source.scoringMethodVersion ?? null, similarity_score: source.score?.normalizedSimilarityScore ?? null, completeness_score: source.score?.completenessScore ?? null, evidence_quality_score: source.score?.evidenceQualityScore ?? null, limitation_count: source.limitations.length }, payloadIntegrityReference: `historical-analogy:${source.sourceRecordId}`, sourceAuditReferences: [source.auditId], finalOutcome: source.status }; }

import { AIAuditRecordType, AIAuditSourceSubsystem, type AIAuditRecordInput, type HistoricalAuditRecord, type HistoricalAuditTranslationContext } from "../../contracts";

function safeCode(value: string, index: number): string { const normalized = value.trim().replace(/[^A-Za-z0-9._:-]+/g, "_").slice(0, 180); return normalized.length === 0 ? `HISTORICAL_REASON_${String(index + 1)}` : normalized; }
export function auditRecordFromHistoricalPattern(source: Readonly<HistoricalAuditRecord>, context: Readonly<HistoricalAuditTranslationContext>): AIAuditRecordInput {
  return { schemaVersion: "1.0", recordId: source.auditId, idempotencyKey: context.idempotencyKey, recordType: AIAuditRecordType.HistoricalRecord, sourceSubsystem: AIAuditSourceSubsystem.HistoricalPatternLibrary, sourceRecordId: source.sourceRecordId, ...(source.recordVersion === undefined ? {} : { sourceRecordVersion: source.recordVersion }), timestamp: source.timestamp, correlationId: source.correlationId, traceId: source.traceId, parentAuditRecordIds: structuredClone(context.parentAuditRecordIds), relatedAuditRecordIds: structuredClone(context.relatedAuditRecordIds), ledgerEntryIds: [], policyVersions: structuredClone(source.policyVersions), status: source.status, reasonCodes: source.reasonCodes.map(safeCode), actor: structuredClone(context.actor), privacyLevel: source.privacyLevel, retention: source.retention, metadata: { ...structuredClone(source.metadata), historical_operation: source.operationType, historical_record_kind: source.recordKind, historical_record_id: source.recordId ?? null, source_reference_count: source.sourceReferenceIds.length, evidence_reference_count: source.evidenceReferenceIds.length }, payloadIntegrityReference: `historical:${source.sourceRecordId}`, sourceAuditReferences: [source.auditId], finalOutcome: source.status };
}

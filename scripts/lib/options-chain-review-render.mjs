import { createHash } from "node:crypto";
const esc = v => String(v ?? "Unknown").replace(/[&<>"']/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[c]).replace(/[\u0000-\u001f\u007f-\u009f\u200e\u200f\u2028-\u202e\u2066-\u2069]/g, c => "\\u" + c.charCodeAt(0).toString(16).padStart(4, "0"));
const usd = v => v === null || v === undefined ? "Unknown" : "$" + Number(v).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 6 });
const cents = v => v === null || v === undefined ? "Unknown" : usd(v / 100);
const number = n => n === null || n === undefined ? "Unknown" : n.toLocaleString("en-US");
const table = (heads, body) => `<div class="scroll"><table><thead><tr>${heads.map(h => `<th scope="col">${esc(h)}</th>`).join("")}</tr></thead><tbody>${body.map(r => `<tr>${r.map(c => `<td>${c}</td>`).join("")}</tr>`).join("")}</tbody></table></div>`;
const css = `:root{color-scheme:light;--ink:#192d39;--muted:#526974;--accent:#165f53;--line:#d7e2e4}*{box-sizing:border-box}html{scroll-padding-top:20px}body{margin:0;background:#f1f5f5;color:var(--ink);font:16px/1.55 system-ui,"Segoe UI",sans-serif}header{background:#142e39;color:white;padding:22px max(24px,calc((100vw - 1220px)/2));display:flex;justify-content:space-between;gap:24px;flex-wrap:wrap}header strong{letter-spacing:.14em}header a{color:#e5f2ec}nav{display:flex;gap:22px;flex-wrap:wrap}main{max-width:1270px;margin:auto;padding:30px 24px 65px}h1{font-size:clamp(28px,4vw,42px);line-height:1.16;margin:12px 0}h2{font-size:24px;margin:0 0 12px}h3{font-size:18px;margin:20px 0 10px}.eyebrow{color:var(--accent);font-size:13px;font-weight:750;letter-spacing:.12em}p{margin:9px 0 16px}a,summary{color:var(--accent)}small,.muted{color:var(--muted)}small{display:block;margin-top:5px;font-size:12px;overflow-wrap:anywhere}.notice{background:#fff4dc;border-left:4px solid #b28325;padding:18px 22px;margin:24px 0}.metrics{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:16px}.metric{background:#fff;border-top:3px solid #81a79b;padding:20px}.metric strong{font-size:29px;display:block;margin:9px 0}.metric span{font-size:13px;color:var(--muted)}section{background:white;padding:28px;margin:24px 0;border:1px solid var(--line);border-radius:6px;min-width:0}.section-head{display:flex;justify-content:space-between;gap:20px;flex-wrap:wrap}.tag{background:#edf3f0;color:var(--accent);padding:4px 10px;border-radius:4px;font-size:13px}table{width:100%;border-collapse:collapse;font-size:13px;text-align:left}th,td{padding:12px;border-bottom:1px solid var(--line);vertical-align:top}th{background:#f5f8f8;color:var(--muted)}td{min-width:75px}td:last-child{min-width:260px;max-width:420px}.scroll{max-width:100%;overflow:auto}.scroll table{min-width:750px}details{margin:16px 0}summary{cursor:pointer;font-weight:650;padding:8px 0}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:12px/1.5 ui-monospace,Consolas,monospace;background:#f4f7f7;padding:14px;max-height:300px;overflow:auto}.positive{color:var(--accent)}.blocked{color:#8b391d}footer{padding:24px;text-align:center;color:var(--muted);font-size:13px}:focus-visible{outline:3px solid #b28325;outline-offset:3px}@media(max-width:800px){.metrics{grid-template-columns:repeat(2,minmax(0,1fr))}section{padding:20px}}@media(max-width:480px){main{padding:22px 12px}.metrics{gap:10px}.metric{padding:14px}.metric strong{font-size:24px}header{padding:18px}section{padding:18px 12px}}@media print{details{display:block}nav{display:none}.scroll{overflow:visible}}`;
const detail = (title, body) => `<details><summary>${esc(title)}</summary><pre>${esc(JSON.stringify(body, null, 2))}</pre></details>`;
function quoteRows(rows) {
  return table(["Contract", "Option bid / ask", "Mark", "Volume", "Reported OI", "Volume / OI", "Review evidence / uncertainty"], rows.map(r => [
    `<b>${esc(r.type.toUpperCase())} ${usd(r.strike)}</b><small>${esc(r.id)}</small>`,
    `${usd(r.bid)} / ${usd(r.ask)}<small>Size ${number(r.bidSize)} / ${number(r.askSize)} contracts</small>`, usd(r.mark), number(r.volume), number(r.openInterest),
    r.volumeOiRatio ? (r.volumeOiRatio.numerator / r.volumeOiRatio.denominator).toFixed(2) + "x" : "Unknown / zero OI",
    `${r.reviewCandidate ? "<b>Activity review candidate</b>" : "Below declared filter"}<small>${esc(r.activityFlags.join("; "))}</small><small>Quote: ${esc(r.quoteUpdatedAt)}</small>${detail("Reasons, hypotheses and missing evidence", { flags: r.activityFlags, positivePeers: r.positivePeerCount, positiveMedian: r.positivePeerMedian, peerRuleEligible: r.peerScreenEligible, quality: r.quality, possibleExplanations: r.possibleExplanations, evidenceNeeded: r.evidenceNeeded, causeConfirmed: false })}`
  ]));
}
export function renderChainReviewBoard(report) {
  const r = report.chain, paper = report.paper?.result, a = paper?.paper.account;
  const styleHash = createHash("sha256").update(css).digest("base64");
  const metric = (label, value, note) => `<div class="metric"><span>${esc(label)}</span><strong>${esc(value)}</strong><small>${esc(note)}</small></div>`;
  const paperSection = paper ? `<p class="tag">Independent ${esc(paper.origin)} account · not Robinhood balances</p><p>Modeled as of ${esc(paper.asOf)}. Saved paper snapshot: ${esc(report.paper.artifactId)}. New candidate: ${paper.candidatePreview ? esc(paper.candidatePreview.trade.tradeId) : "none"}.</p>
    <div class="metrics">${metric("Conservative settled cash", cents(paper.reconciliation.conservativeAvailableSettledCashCents), "No reuse of unsettled proceeds")}${metric("Original paper equity", cents(a.equityCents), "Prior/slippage-adjusted model mark")}${metric("Portfolio equity", cents(paper.reconciliation.portfolioEquityCents), "Qualified bid basis; may be unknown")}${metric("Reviews / candidate lessons", `${paper.paper.reviews.length} / ${paper.paper.mistakeNotebook.entries.length}`, "Existing records are preserved")}</div>
    <h3>Entry review blockers</h3>${paper.blockers.length ? table(["Source", "Blocker", "Applies to"], paper.blockers.map(b => [esc(b.source), `<b class="blocked">${esc(b.code)}</b>`, esc(b.subject)])) : "<p>No blockers under declared assumptions. This is not a trade authorization.</p>"}
    ${detail("Fee reconciliation and unrecorded candidate preview", { reconciliation: paper.reconciliation, candidatePreview: paper.candidatePreview })}
    ${table(["Original case", "ETF", "Disposition", "Review"], paper.inventory.map(i => [esc(i.tradeId), esc(i.symbol), esc(i.status), esc(i.reviewId)]))}` : "<p>No saved modeled account was attached. Account diagnostics remain unknown.</p>";
  const expirySections = r.expirations.map(e => {
    const rs = r.rows.filter(x => x.symbol === e.symbol && x.expiry === e.expiry), flagged = rs.filter(x => x.reviewCandidate).sort((a, b) => (b.volume ?? -1) - (a.volume ?? -1) || a.id.localeCompare(b.id));
    return `<section id="${esc(e.symbol + "-" + e.expiry)}"><div class="section-head"><h2>${esc(e.symbol)} · ${esc(e.expiry)}</h2><span class="tag">${e.comparisonOnly ? "Later-expiry comparison" : "Requested expiry"}</span></div>
      <p>${number(e.contractCount)} contracts · ${number(e.strikeCount)} strike levels · ${usd(e.minStrike)}–${usd(e.maxStrike)} strikes · ${number(e.activityCandidates)} activity review candidates.</p>
      <p class="muted">Quote-refresh date(s): ${esc(e.quoteRefreshDates.join(", "))}. Independent volume-session and OI dates were not supplied. Reported volume sum: ${number(e.reportedVolumeSum)}; mixed refresh times are not a synchronized end-of-day total.</p>
      <details><summary>Review all ${number(flagged.length)} flagged contracts</summary>${flagged.length ? quoteRows(flagged) : "<p>No contract meets this filter. This does not prove ordinary activity.</p>"}</details>
      <details><summary>Show the complete ${number(rs.length)}-contract chain</summary>${quoteRows(rs)}</details></section>`;
  }).join("");
  const meetingNote = r.scope.expirationEnd === "2026-09-16" ? `<section id="interpretation"><h2>What can explain the patterns?</h2>
    <p><b>Strike range:</b> exchanges list series under different weekly/daily/monthly programs and add strikes over time. Nasdaq added Tuesday/Thursday GLD expirations and Monday/Wednesday IBIT expirations in August 2026. The September 18 date falls on the third Friday; broader established series are consistent with its larger strike range. Individual listing dates and addition reasons are absent, so the exact cause of each range remains unverified. A $900 strike is not a $900 forecast. <a href="https://www.nasdaqtrader.com/MicroNews.aspx?id=OTA2026-32">Nasdaq listing notice</a>.</p>
    <p><b>Event exposure:</b> the FOMC calendar lists September 15–16 with economic projections. An option expiring before September 16 can express expectations beforehand but cannot remain open for that day's outcome. September 16 and 18 expirations can include that event date. This does not prove that their volume was caused by FOMC positioning. <a href="https://www.federalreserve.gov/monetarypolicy/fomccalendars.htm">Federal Reserve calendar</a>.</p>
    <p><b>September 4 context:</b> BLS published the August employment report that morning. Macro repricing is one hypothesis for that session's activity; proving it requires timestamped trades and price/volatility changes. The snapshot does not identify the trader or motivation. <a href="https://www.bls.gov/news.release/archives/empsit_09042026.htm">BLS release</a>.</p>
    <p><b>Volume is not a directional vote:</b> large call or put volume may reflect buying, selling, hedging, closing or linked spreads. OI is a different measure. Even next-session OI changes do not identify a specific buyer or trade cause. <a href="https://www.optionseducation.org/news/open-interest-why-it-matters">OIC explanation</a>.</p></section>` : "";
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'sha256-${styleHash}'; base-uri 'none'; form-action 'none'"><title>Alpha · Option activity and account review</title><style>${css}</style></head><body>
    <header><strong>ALPHA</strong><nav><a href="#scope">Expiry map</a><a href="#paper">Account review</a><a href="#interpretation">Interpretation</a><a href="report.json" download>Save complete report</a></nav></header><main>
    <span class="eyebrow">GLD + IBIT / LOCAL REVIEW BOARD</span><h1>Option activity, with the limits visible.</h1><p>Expirations ${esc(r.scope.expirationStart)} through ${esc(r.scope.expirationEnd)}; later comparisons are marked separately.</p>
    <p class="muted">Captured ${esc(r.capturedAt)} · Board constructed ${esc(report.createdAt)}</p>
    <div class="notice"><b>Historical source snapshots. No future-session prices.</b><p>Activity flags are review candidates, not confirmed statistical anomalies or trading signals. Prices are per-share option premiums; strikes are exercise prices. Zero-sided quotes do not establish a usable market. Market contracts are not inserted into the separate simulated account.</p></div>
    <div class="metrics">${metric("Contracts retained", number(r.instrumentCount), "Every returned contract remains in the report")}${metric("Activity review candidates", number(r.flaggedCount), "Declared thresholds; no historical calibration")}${metric("Stale quote snapshots", number(r.rows.filter(x => x.quality.includes("STALE_AT_ASSESSMENT")).length), "Staleness is measured at board construction")}${metric("Automatic orders", "Off", "No account or order access")}</div>
    <section id="scope"><h2>Expiration and listed strike map</h2><p>These are listed contract ranges, not expected ETF highs or lows. Collection status: ${esc(r.status)}.</p>
      ${table(["ETF / expiry", "Listed strike range", "Strike levels", "Contracts / quotes", "Flagged"], r.expirations.map(e => [`<a href="#${esc(e.symbol + "-" + e.expiry)}">${esc(e.symbol)} ${esc(e.expiry)}</a>${e.comparisonOnly ? "<small>Comparison after scope end</small>" : ""}`, `${usd(e.minStrike)}–${usd(e.maxStrike)}${!e.listingComplete ? "<small>Partial listing</small>" : ""}`, number(e.strikeCount), `${number(e.contractCount)} / ${number(e.quotedContracts)}`, number(e.activityCandidates)]))}
      ${detail("Screen rules, scope gaps and limitations", { policy: r.policy, issues: r.issues, limitations: r.limitations })}</section>
    <section id="paper"><h2>Modeled account and candidate risk review</h2>${paperSection}</section>${meetingNote}${expirySections}
    <section><h2>Evidence retained</h2><p><a href="capture.json" download>Exact saved market capture</a> · <a href="manifest.json" download>Snapshot manifest</a>. This board can be verified in a fresh process using its original readers. It does not refresh market data or inspect current brokerage balances.</p>${detail("Fingerprints", { chain: r.reportFingerprint, paper: report.paper?.artifactFingerprint ?? null, board: report.reportFingerprint })}</section>
    </main><footer>Alpha · Protect capital · Causes unconfirmed · No automatic orders</footer></body></html>\n`;
}
